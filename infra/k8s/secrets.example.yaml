apiVersion: v1
kind: Secret
metadata:
  name: nexusai-secrets
  namespace: nexusai-gateway
type: Opaque
stringData:
  JWT_SECRET: replace-with-strong-secret
  INTERNAL_SERVICE_TOKEN: replace-with-internal-token
  POSTGRES_PASSWORD: platform
